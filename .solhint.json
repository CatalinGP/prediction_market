{
    "extends": "solhint:recommended",
    "rules": {
      "compiler-version": ["error", ">=0.7.3 <0.9.0"],
      "func-visibility": ["warn", { "ignoreConstructors": true }],
      "no-inline-assembly": "error",
      "reason-string": ["warn", { "maxLength": 64 }]
    }
  }
